# 로또 조합기 앱 설치하기

앱은 `docs/` 폴더 안에 다 들어있습니다. 이걸 인터넷에 한 번 올리면 휴대폰에서 주소로 열고
"홈 화면에 추가"만 하면 됩니다. 그다음부터는 **인터넷 없어도** 작동합니다.

방법은 두 가지. 아무거나 하나 고르세요.

---

## 방법 A. Netlify — 가장 빠름 (5분, 드래그로 끝)

1. https://app.netlify.com/drop 접속
2. 처음이면 이메일로 무료 회원가입 (구글 계정으로 바로 가능)
3. 화면의 점선 네모 칸에 **`docs` 폴더를 통째로 드래그** 해서 놓기
4. 잠시 뒤 `https://무작위이름.netlify.app` 주소가 나옵니다 → 이게 앱 주소
5. (선택) Site configuration → Change site name 에서 주소를 원하는 이름으로 변경

**앱을 고쳤을 때 업데이트:** 같은 사이트의 Deploys 탭에 `docs` 폴더를 다시 드래그.

---

## 방법 B. GitHub Pages — 한 번 세팅하면 `배포.bat` 더블클릭으로 자동 업데이트

한 번만 아래 6단계를 하면 됩니다. 10~15분.

### 1단계 — GitHub 회원가입

1. https://github.com/signup 접속
2. 이메일, 비밀번호, 아이디(영문) 입력 → 안내대로 가입
3. 이메일 인증까지 완료

### 2단계 — 저장소(repository) 만들기

1. 로그인한 상태에서 https://github.com/new 접속
2. **Repository name** 칸에 `lotto` 입력 (다른 이름도 됨)
3. 그 아래 **Public** 에 점 찍기 (Private 아님 — 무료 Pages 는 Public 필요)
4. 나머지는 건드리지 말고 초록색 **Create repository** 클릭
5. 다음 화면 위쪽에 나오는 주소를 복사합니다.
   `https://github.com/내아이디/lotto.git` 처럼 생겼습니다. (`.git` 으로 끝나는 것)

### 3단계 — 컴퓨터에서 GitHub 로 올리기

1. 프로젝트 폴더(`로또`)의 **`깃허브연결.bat` 더블클릭**
2. 검은 창이 뜨면 방금 복사한 주소를 **붙여넣고** Enter
   (붙여넣기: 마우스 오른쪽 클릭 또는 Ctrl+V)
3. 잠시 뒤 브라우저에 **GitHub 로그인 창**이 뜹니다 → 로그인하고 **Authorize/허용**
4. 창에 에러 없이 끝나면 성공. (다시 실행할 필요 없음)

> 안 되면: GitHub 사이트의 `lotto` 저장소 페이지를 새로고침 해서 파일들이 올라왔는지 확인하세요.

### 4단계 — GitHub Pages 켜기

1. GitHub 에서 `lotto` 저장소 페이지 열기
2. 위쪽 탭 중 **Settings**(톱니바퀴) 클릭
3. 왼쪽 메뉴에서 **Pages** 클릭
4. **Source** 를 **Deploy from a branch** 로 둔 상태에서
5. **Branch** 를 **main**, 그 옆 폴더를 **/docs** 로 선택 → **Save**

### 5단계 — 주소 확인

1~2분 기다렸다가 4단계 화면을 새로고침 하면 맨 위에
`Your site is live at https://내아이디.github.io/lotto/` 가 나옵니다.
이 주소가 **앱 주소**입니다.

### 6단계 — 휴대폰에 설치

아래 "휴대폰에 아이콘으로 설치" 참고.

---

### 앱을 고쳤을 때 (2번째부터)

프로젝트 폴더의 **`배포.bat` 더블클릭** 하나면 끝입니다.
(데이터 갱신 + 캐시 버전 올림 + GitHub 자동 반영. 1~2분 뒤 휴대폰 앱도 자동 업데이트)

---

## 휴대폰에 아이콘으로 설치

앱 주소를 휴대폰 브라우저에서 연 다음:

### 아이폰 (Safari)
1. 아래 가운데 **공유 버튼**(네모에 화살표) 탭
2. **홈 화면에 추가** 탭 → 추가
3. 홈 화면에 생긴 아이콘으로 실행 (주소창 없는 앱처럼 열림)

### 안드로이드 (Chrome)
1. 오른쪽 위 **⋮** 메뉴 탭
2. **앱 설치** 또는 **홈 화면에 추가** 탭 → 설치
3. 생긴 아이콘으로 실행

설치 후 한 번 실행해두면, 이후에는 인터넷이 없어도 아이콘을 눌러 번호를 뽑을 수 있습니다.
새 회차 당첨번호만 인터넷이 있을 때 "회차" 탭에서 받아오면 됩니다 (없으면 직접 입력).
