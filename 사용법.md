# 로또 번호 조합기 — 사용법

## 휴대폰 앱 (기본)

설치는 **`앱_설치.md`** 를 보세요. 한 번 설치하면 인터넷 없어도 됩니다.

### 화면 4개 (아래 탭)

- **뽑기** — 버튼 한 번 누르면 20게임이 나옵니다. `[사진으로 저장]` 을 누르면 사진 앱(갤러리)에 저장됩니다.
  마음에 드는 게임 옆 자물쇠(🔓)를 눌러 고정하고 `[고정 안 한 것만 다시 뽑기]` 를 누르면 나머지만 다시 나옵니다.
- **회차** — 저장된 당첨번호 확인.
  `[인터넷에서 새 회차 가져오기]` : 인터넷 연결됐을 때 최신 회차 자동 저장.
  `[직접 입력]` : 인터넷 없어도 회차·날짜·번호를 손으로 넣어 저장.
- **기록** — 지금까지 뽑은 조합이 자동으로 저장됩니다. 눌러서 펼치고 다시 사진으로 저장 가능.
- **설정** — 각 그룹 게임 수(기본 4 / 4 / 2 / 5 / 5)와 '최근 통계' 기준 회차 수(기본 100)를 조절.
  `[기본값으로 되돌리기]` 로 원상복구.

### 나오는 번호 (기본 20게임)

- **1~4**: 직전 회차와 겹치는 숫자 0개
- **5~8**: 직전 회차와 겹치는 숫자 1개
- **9~10**: 직전 회차와 겹치는 숫자 2개
- **11~15**: 최근 100회차 통계 (많이 나온 15개 중 2 + 적게 나온 15개 중 2 + 나머지 15개 중 2)
- **16~20**: 전체 회차 통계 (같은 방식)

## 데스크톱 버전 (보조)

PC에서 `로또번호.bat` 더블클릭 → 20게임 PNG 생성 + (카카오 연결 시) 카카오톡 전송.
자세한 건 `workflows/lotto_weekly.md`, 카카오 연결은 `kakao_setup.md`.

## 주의

생성되는 번호는 무작위이며 당첨을 보장하지 않습니다. 구매는 본인 판단으로 하세요.
