# 보안 점검 결과 (2026-09-10)

전체 코드 + API 사용 + 공개 저장소 노출을 점검하고 아래를 조치했습니다.

## 이상 없음

- **비밀 파일 노출 없음** — git 이력 전체에 `.env`, `credentials.json`, `token.json`,
  `kakao_token.json` 이 커밋된 적 없음. `.gitignore` 로 계속 차단됨.
- **하드코딩된 키/비밀번호 없음** — 모든 코드/설정 파일 확인.
- **웹앱 XSS 없음** — 앱은 모든 화면 출력을 `textContent` 로만 함(HTML 주입 경로 없음).
  `eval`, `document.write`, `innerHTML=사용자데이터` 없음.

## 조치한 것

| 항목 | 내용 |
|---|---|
| 웹앱 CSP 추가 | `index.html` 에 Content-Security-Policy. `script-src 'self'` 로 인라인 스크립트·eval 차단, `connect-src` 를 자기 자신 + 로또 미러로만 제한, `object-src/base-uri 'none'`. |
| 카카오 OAuth CSRF 방지 | `kakao_auth.py` 에 `state` 난수 파라미터 추가·검증. 콜백 경로 검사. 로컬 서버가 엉뚱한 요청을 무시하고 계속 대기. 루프백(127.0.0.1) 고정. |
| 토큰 파일 권한 | `kakao_token.json`, `token.json` 저장 시 `chmod 600`(소유자만). |
| 구글 권한 최소화 | `drive`(전체 드라이브) → `drive.file`(앱이 만든 파일만) 로 축소. |
| 외부 데이터 방어 | `lottery-api.js` 응답 크기 상한(3MB), 회차 번호 범위 검증, `redirect:"error"`, `referrer:no-referrer`. 받은 데이터는 저장 전 `isValidDraw` 로 형식 검증(기존). |
| 배치 파일 | `깃허브연결.bat` 의 URL 입력을 따옴표로 감쌈. |
| 죽은 코드 제거 | `app.js` 의 미사용 `innerHTML` 분기 삭제. |

## 남은 위험 (감수 가능 수준)

- **로또 데이터 출처가 제3자 미러**(`smok95.github.io/lotto`). 동행복권 공식 API 가 막혀서 대안이 없음.
  해당 계정이 조작된 데이터를 주더라도 **코드 실행은 불가**하고, 형식 검증을 통과한 잘못된 당첨번호가
  추천 번호의 기준이 될 뿐임(당첨/개인정보와 무관). 공식 API 가 살아나면 교체 권장.
- **공개 GitHub 저장소에 커밋 이메일(min2811@gmail.com)이 남음.** 보안 문제는 아니고 스팸 노출 가능성.
  원하면 GitHub 설정에서 noreply 이메일로 바꾸고 `git config user.email` 을 그 주소로 변경(이후 커밋부터 적용).
- 카카오/구글 토큰은 로컬 디스크에 평문 저장(권한 600). 1인 데스크톱 기준 허용 범위.
