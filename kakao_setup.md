# 카카오톡으로 번호 받기 — 최초 1회 설정

번호를 카카오톡 "나에게 보내기"로 받으려면 아래를 한 번만 해두면 됩니다. 약 10분.
설정을 안 해도 번호 생성과 PNG 만들기는 정상 작동하며, 그 PNG를 직접 카톡에 공유하면 됩니다.

## 1. 카카오 개발자 앱 만들기

1. https://developers.kakao.com 접속 → 카카오 계정으로 로그인
2. 오른쪽 위 **내 애플리케이션** → **애플리케이션 추가하기**
3. 앱 이름(예: `로또번호`), 회사명(아무거나) 입력 후 저장

## 2. 필요한 값 복사해서 .env에 넣기

1. 방금 만든 앱 클릭 → 왼쪽 메뉴 **앱 키**
2. **REST API 키** 를 복사
3. 프로젝트 폴더의 `.env` 파일을 열어 아래 줄에 붙여넣기
   ```
   KAKAO_REST_API_KEY=여기에_붙여넣기
   ```
   (`.env` 파일이 없으면 `.env.example` 을 복사해서 이름을 `.env` 로 바꾸세요.)

## 3. 로그인 주소(Redirect URI) 등록

1. 왼쪽 메뉴 **카카오 로그인** → **활성화 설정** 을 **ON**
2. 같은 화면 아래 **Redirect URI** 에 정확히 이렇게 등록:
   ```
   http://localhost:8321
   ```

## 4. 메시지 전송 권한 켜기

1. 왼쪽 메뉴 **카카오 로그인** → **동의항목**
2. **접근권한 관리 → 카카오톡 메시지 전송** 을 찾아 **사용 설정**
   (개인용 "나에게 보내기"는 이 설정만으로 됩니다. 별도 심사 필요 없음)

## 5. 연결 실행

터미널(또는 `카카오연결.bat` 더블클릭)에서:

```
.venv\Scripts\python.exe -m tools.kakao_auth
```

브라우저가 열리면 카카오 로그인 → **동의하기**. "연결 완료" 가 뜨면 끝.
이제 `로또번호.bat` 를 실행하면 번호가 카카오톡으로 옵니다.

## 문제가 생기면

- **KOE006 / redirect_uri 오류** → 3번의 Redirect URI가 `http://localhost:8321` 과 정확히 같은지 확인
- **KOE205 / 동의항목 오류** → 4번의 메시지 전송 권한이 켜져 있는지 확인
- **토큰 만료** → `python -m tools.kakao_auth` 를 다시 실행하면 됩니다
